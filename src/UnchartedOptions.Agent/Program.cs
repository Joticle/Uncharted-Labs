// Uncharted Options -- entrypoint.
//
// Single-shot by design: one evaluation cycle, then exit. The strategy, the gates and the
// broker adapter arrive in the commits that follow.

Console.WriteLine("Uncharted Options");
return 0;
